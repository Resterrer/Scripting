using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cricket : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       Destroy(gameObject);
      //уничтожение обьекта в игре
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}